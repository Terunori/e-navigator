%h2 面接一覧

- if params[:user_id].to_i == current_user.id then
  %table.table.table-hover
    %tr
      %th.col-xs-3 面接開始時間
      %th.col-xs-3 面接官
      %th.col-xs-2 承認状態
      %th.col-xs-1
      %th.col-xs-1
    - current_user.interviews.each do |intview|
      %tr
        %td= intview.begin_at
        %td= intview.interviewer.email
        %td= intview.allowed
        %td edit
        %td delete
